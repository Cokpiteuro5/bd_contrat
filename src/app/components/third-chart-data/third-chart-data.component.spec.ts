import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ThirdChartDataComponent } from './third-chart-data.component';

describe('ThirdChartDataComponent', () => {
  let component: ThirdChartDataComponent;
  let fixture: ComponentFixture<ThirdChartDataComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ThirdChartDataComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ThirdChartDataComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});












































































































